# Apply Progress — `canonical-domain-models-and-capabilities`

## Status

Completed.

## Tasks Completed

- [x] Crear `QualifiedName` en `domain/schema/qualified_name.py`.
- [x] Crear `SqlType` y `TypeFamily` en `domain/schema/types.py`.
- [x] Extender `ColumnSnapshot` y `ColumnAttributes` con `default_expression`, `is_identity` y `collation`.
- [x] Crear `ProviderCapabilities` y `ComparisonMode` en `domain/capabilities.py`.
- [x] Re-exportar abstracciones canónicas en `domain/__init__.py`.
- [x] Añadir pruebas unitarias en `test_canonical_models.py`.
- [x] Ejecutar la suite completa de pruebas de pytest y asegurar 0 fallos.
